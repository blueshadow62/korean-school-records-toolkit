# 학교생활기록부 도구 사용자 설명서

실제로 학생부를 쓰고 점검하는 선생님을 위한 사용법 안내입니다. 설치 방법은 다루지 않습니다 — 설치가 필요하면 `INSTALL.md`(Codex) 또는 `README_CLAUDE_CODE.md`(Claude Code)를 먼저 참고하세요.

## 이 도구로 할 수 있는 일

이 툴킷은 서로 독립된 두 가지 기능을 묶어 놓은 것입니다. 한 가지만 요청하면 그 기능만 실행되고, 다른 기능은 자동으로 따라 실행되지 않습니다.

| 기능 | 무엇을 해주나요 | 이렇게 물어보세요 |
|---|---|---|
| **학생부 작성 도우미** | 교사가 실제 관찰·평가한 사실을 바탕으로 세특·창체 특기사항·행특 초안을 만들거나 첨삭·점검 | "다음 관찰 내용으로 국어 세특 초안 써줘", "이 문장 금지 표현 있는지 점검해줘" |
| **글자 수 계산** | 텍스트의 글자 수·줄 수·byte 수를 추정이 아니라 코드로 정확히 계산 | "이거 500자 넘는지 정확히 세줘", "NEIS 기준 byte로 계산해줘" |

## 두 기능을 함께 쓸 때 추천 순서

```mermaid
flowchart LR
    A[교사 관찰·평가 자료] --> B["① 세특·창체·행특 초안"]
    B --> C["② 글자 수 · NEIS Byte 확인"]
    C --> D[교사 최종 확인 후 NEIS 입력]
```

"초안부터 글자 수까지 한 번에 해줘"처럼 통합 요청도 가능합니다. 이때도 각 단계 결과를 그대로 이어 붙이지 않고, 교사가 확인해야 할 사항을 단계마다 남깁니다.

## ① 학생부 작성 도우미 (세특 · 창체 · 행특)

### 이렇게 물어보세요
- "다음은 제가 관찰한 내용입니다 (자료 붙여넣기). 이걸로 국어 세특 초안 써줘."
- "이 문장, 학생부 금지 표현이나 분량 기준에 걸리는지 점검해줘."
- "행동특성 및 종합의견 다듬어줘."
- "2026학년도 새 기재요령으로 등록하고 활성화해줘." (학년도가 바뀌었을 때)

### 결과가 어떻게 나오나요
자료(교사 관찰, 검토된 결과물, 확인된 평가 결과 등)가 충분하면 완성된 초안(`[세특 초안]`)과 어떤 근거를 썼는지, 교사가 추가로 확인할 사항이 함께 나옵니다.

자료가 부족하면 **완성된 문장 대신** `[학생별 사실 미확인 — 그대로 입력하지 않는 작성 후보]` 표시와 함께 빈칸 채우기 형태의 작성 틀만 나옵니다. 이건 오류가 아니라 확인되지 않은 사실을 완성문처럼 보이게 만들지 않기 위한 안전장치입니다. "그래도 완성본으로 줘"라고 요청해도 자료가 없으면 완성형으로 바뀌지 않습니다.

### 결과를 그대로 NEIS에 입력하면 안 되는 이유
- AI가 만든 문장은 초안일 뿐입니다. 선생님이 사실·과장·금지 표현을 최종 대조하기 전까지는 입력용 확정문이 아닙니다.
- 다음 내용은 자동으로 제외됩니다(넣어 달라고 요청해도 반영되지 않습니다): 공인어학시험, 교내외 대회·수상, 모의고사 성적, 논문·도서 실적, 지식재산권, 해외 활동, 구체적인 대학명·기관명·강사명·자격증명, 학생을 특정할 수 있는 재학 학교명.
- 교사가 직접 관찰하지 못한 학생의 내면·가정 형편·교외 성과는 단정해서 쓰지 않습니다.
- 대학명, 합격 가능성, 입시 유불리와 관련된 표현은 공식 기록에 넣지 않습니다.

## ② 글자 수 계산

### 이렇게 물어보세요
- "이 문장 글자 수, 줄 수, byte 수 알려줘."
- "NEIS 기준 byte로 계산해줘." — 한글 3byte / 영문 1byte / 줄바꿈 2byte로 세는 실제 나이스 입력 화면 기준입니다.

기본은 일반 글자 수 계산(`default`)이고, NEIS 제출용 byte 계산이 필요할 때만 "NEIS 기준"이라고 말하면 됩니다. 한글·영문·공백·개행·이모지가 섞여 있어도 코드로 정확히 세므로 눈대중과 달리 1자 차이도 놓치지 않습니다.

## 안전 원칙 요약

- 이 도구를 활용하실 때에는 근무하시는 시도교육청의 AI 활용 지침 등을 확인하시기 바랍니다.
- 실제 학교 교육활동에서 교사가 직접 관찰·평가한 사실만 사용합니다.
- 확인되지 않은 활동·성과를 사실처럼 쓰지 않습니다.
- 최종 준수 여부는 항상 교사가 원자료·학교 규정과 대조해 판단합니다. 이 도구는 판단을 보조할 뿐 대신하지 않습니다.
- 학생 실명 자료는 예제·테스트에 쓰지 않으며, 배포 전 개인정보는 제거되어 있습니다.

## 라이선스

| 기능 | 라이선스 | 저작권 표기 |
|---|---|---|
| 학생부 작성 도우미 | CC BY-NC-SA 4.0 — 비영리·동일조건변경허락·저작자표시 | © 2026 류기현(부산 동아공고) |
| 글자 수 계산 | MIT — 자유롭게 사용·수정·재배포 | 표기 없음 |

학생부 작성 도우미는 **비영리 목적(학교 등 교육기관의 내부 사용 포함)에 한해** 자유롭게 쓰고 고칠 수 있습니다. 수정본을 배포할 때도 같은 라이선스를 적용해야 하고, 원저작자 표시를 유지해야 합니다. 상업적 이용은 금지되며, 문의는 이 저장소의 GitHub Issue로 받습니다.

## 문제가 있을 때

저장소 Issue에 남겨 주세요. 실제 학생 정보는 어떤 경우에도 포함하지 마세요.
